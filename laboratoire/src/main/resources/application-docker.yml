server:
  port: 8071
eureka:
  client:
    service-url:
      defaultZone: http://discovery-server:8761/eureka


spring:
  application:
    name: laboratoires
  datasource:
    url: jdbc:postgresql://laboratoires-db:5433/laboratories
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${app.keycloak.serverUrl}/realms/${app.keycloak.realm}

app:
  keycloak:
    realm: ${APP_KEYCLOAK_REALM}
    serverUrl: ${APP_KEYCLOAK_SERVER_URL_DKR}

application:
  config:
    laboratoires-url: http://gateway:8080/api/v1/laboratoires

management:
  tracing:
    sampling:
      probability: 1.0