server:
  port: 8075
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka


spring:
  application:
    name: tests
  datasource:
    url: jdbc:postgresql://localhost:5437/tests
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql

application:
  config:
    tests-url: http://localhost:8080/api/v1/tests

management:
  tracing:
    sampling:
      probability: 1.0